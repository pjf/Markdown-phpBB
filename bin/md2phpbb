#!/usr/bin/perl -w
use 5.010;
use strict;
use warnings;
use autodie;

# ABSTRACT: Turn markdown into phpBB code
# PODNAME: md2phpbb
# VERSION

use Markdown::phpBB;

my $md2phpbb = Markdown::phpBB->new;

my $text = join('', <>);

print $md2phpbb->convert($text);
